<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pulsante per iniziare la creazione di un nuovo evento -->
    <item
        android:id="@+id/nav_create_event"
        android:icon="@drawable/ic_add_marker"
        android:title="Crea" />

    <!-- Voce per tornare alla mappa principale (Home) -->
    <item
        android:id="@+id/nav_home"
        android:icon="@drawable/ic_home"
        android:title="Home" />

    <!-- Voce per accedere alla schermata delle chat -->
    <item
        android:id="@+id/nav_chat"
        android:icon="@drawable/ic_chat"
        android:title="Chat" />

    <!-- Voce per accedere al proprio profilo -->
    <item
        android:id="@+id/nav_profile"
        android:icon="@drawable/ic_profile"
        android:title="Profilo" />

</menu>